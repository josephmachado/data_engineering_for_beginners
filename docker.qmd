# Use [Docker] to ensure your code is able to run on any machine

## Create a blueprint of what you want your operating system to be with a docker image

## Use a docker image to start running an operating system (aka a container)

## Docker enables you to run multiple containers

## Docker compose provides a way to define which and how to run containers

